name: trap-signal
version: 0.1.0

authors:
  - mosop

targets:
  trap-signal:
    main: src/trap-signal.cr

crystal: 0.20.0

license: MIT

dependencies:
  cli:
    github: mosop/cli
    version: 0.3.1.1
